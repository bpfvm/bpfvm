test/*.a
test/*.o
test/*.so
test/*.out
test/*.linked
test/*.host
build/
root/

.*~

# busybox 上游源码树（本地 clone，不纳入版本控制；构建由根目录 build_busybox.sh 驱动）
busybox/
# openssl 上游源码树（本地 clone，不纳入版本控制；构建由 scripts/build_openssl.sh 驱动）
openssl/
**/bpfvm.bpf
